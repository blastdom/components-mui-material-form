import { ToggleButtonGroupElement } from 'react-hook-form-mui';
import { createBDomComponent } from 'blastdom';

export default createBDomComponent(
  ToggleButtonGroupElement,
  'mui.m.f.toggle.button.group.element',
  {},
);
