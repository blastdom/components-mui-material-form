import { DateTimePickerElement } from 'react-hook-form-mui/date-pickers';
import { createBDomComponent } from 'blastdom';

export default createBDomComponent(
  DateTimePickerElement,
  'mui.m.f.date.time.picker.element',
  {},
);
